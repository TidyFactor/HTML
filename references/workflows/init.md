# Workflow: init

Scaffolds a 100% static HTML/CSS/JS site with clean directory layout and zero server runtime.

---

## Steps

0. **Step 0: CDL Resolution & Brief Check**:
   - Check `.tidyfactor/html-brief.md`. If missing, apply default `web-components` and `vanilla-css`.

1. **Scaffold Directory Tree**:
   - Create `index.html`, `css/style.css`, `css/reset.css`, `js/main.js`, and `assets/`.

2. **Inject Semantic Structure**:
   - Assemble header, navigation, main content sections, and footer with accessible markup.

3. **Pre-Emit Self-Critique**:
   - `/* Pre-emit critique: P5 H5 E5 S5 R5 V5 D5 */`

---

## Validation checklist

- [ ] Zero server runtime required; runs directly in any browser.
- [ ] Semantic HTML5 elements used throughout.
- [ ] Relative links used for all stylesheets and scripts.
- [ ] Pre-emit critique stamp included.
